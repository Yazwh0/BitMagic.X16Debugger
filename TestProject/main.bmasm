.machine CommanderX16R42

    lda #01
    pha
    lda #02
    pha
    lda #03
    pha

    lda #$00
    sta ADDRx_H
    lda #$00
    sta ADDRx_M
    lda #$10
    sta ADDRx_L

    lda #$ff
    sta DATA0

.loop:
    lda #0
    inc $01
    jmp loop
